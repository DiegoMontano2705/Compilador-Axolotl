programa simpleClass;

clase Producto {
    atributos
        flotante : descuento, precio;

    metodos
        flotante funcion precioConDescuento(){
            %%descuento = precio * 0.20;
            %%regresa(precio-descuento);
            regresa(precio);
        } 
};

entero funcion pruebaUno(){
    regresa(10);
}

principal() {
    variables entero : x;
    x = 10;
}