programa simpleClass;

clase Producto {
    atributos
        flotante : descuento, precio;

    metodos
        flotante funcion precioConDescuento(){
            regresa(precio);
        } 
};

entero funcion pruebaUno(){
    variables Producto : z;
    regresa(10);
}

principal() {
    variables Producto : x, y;  
}