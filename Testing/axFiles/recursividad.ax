programa recursividad;

void funcion pruebaUno(entero x) {
    si (x > 0) entonces {
        escribe(x);
        x = x - 1;
        pruebaUno(x);
    }
}

%%test comment
principal() {
    pruebaUno(10);
}