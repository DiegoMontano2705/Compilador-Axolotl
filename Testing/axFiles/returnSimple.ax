programa returnSimple;

entero funcion pruebaUno(entero x) {
    variables entero : a, b, c;
    a = 100;
    b = 200;
    si (a>0) entonces {
        regresa(300);
    }
    escribe(b);
}

%%test comment
principal() {
    variables entero : z, a;
    a = 100;
    z = pruebaUno(a+1);
    escribe(z);
}