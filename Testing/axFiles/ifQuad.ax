programa ifQuad;
variables entero : a,b,c,d;

%%if comment
principal() {
    a=10;
    b=11;
    d=2;
    si (a > 1) entonces {
        c = a * d;
    } sino {
        c = d + a;
    }
    d = c;
    escribe(d);
}