programa test2;
variables entero : a,b,f;

%%test comment
principal() {
    variables flotante : x,y,z;
    a = 4; %% una igualacion
    b = 5 + 5 + 4 - 2;
    f = 8 * 2;
    f  = (5 + 6 * 100);
    escribe(a);
    escribe(b);
    escribe(f);
}