programa test1;
variables entero : a,b,d;

void funcion pruebaUno(entero e, flotante f) {
    b = b + 6;
    regresa (5);
}

entero funcion pruebaDos(entero e, flotante f, char x) {
    b = b + 6;
    regresa (5);
}

%%test comment
principal() {
    a = 4; %% una igualacion
    b = 5 + 5 + 4 - 2;
    d = 8 * 2;
    d  = (5 + 6 * 100);
    pruebaUno(1+1,20.4);
}