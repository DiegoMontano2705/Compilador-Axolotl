programa rec;
variables entero : a, b;

entero funcion test(entero x) {
    variables flotante : z;
    variables entero : alfa;
    alfa = 77;
    z = 4.2;
    escribe(z);
    escribe(x);
    escribe(a);
    regresa(1000);
}

principal() {
    escribe(test(10));
}