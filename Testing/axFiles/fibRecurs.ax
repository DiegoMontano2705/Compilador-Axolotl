programa fibRecurs;

%% Fibonnaci with recursion
entero funcion fib(entero x) {
    si (x < 1) entonces{
        regresa(x);
    }
    fib(x-2);
}


principal() {
    variables entero : n,ans;
    lee(n);
    %% ans = fib(n);
    escribe(fib(n));
}