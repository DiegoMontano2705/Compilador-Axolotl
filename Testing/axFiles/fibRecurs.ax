programa fibRecurs;

%% Fibonnaci with recursion

entero funcion fib(entero x) {
    si (x < 2) entonces {
        regresa(x);
    }
    fib(x-2);
}


principal() {
    variables entero : n,ans;
    n = 9;
    ans = fib(n);
    escribe(ans);
}